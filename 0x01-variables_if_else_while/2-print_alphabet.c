#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - print alphabets in lower case
 * Return: Always 0 (success)
 */
int main(void)
{
	char lc;
	
	for (lc = 'a'; lc <= 'z'; lc++);
	{
		putchar(lc);
	}
	putchcar('\n');

	return (0);
}
