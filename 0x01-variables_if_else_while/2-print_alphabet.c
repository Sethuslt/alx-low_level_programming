#include<stdlib.h>
#include<time.h>
#include<stdio.h>
/**
* main - main function
* Return: Always 0 (success)
*/
int main(void)
{
	char y;

	for (y = 'a'; y <= 'z'; y++)

	putchar(y);

	putchar('\n');

	return (0);
}
